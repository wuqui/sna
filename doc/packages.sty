    % essential

      \usepackage[T1]{fontenc}
      \usepackage[utf8]{inputenc}
      \usepackage{lmodern}
      \usepackage[british]{babel}
      \usepackage{csquotes}
      \usepackage{graphicx}
      \usepackage{textcomp}

    % misc

      % ignore non-breakable space utf8 error
      \usepackage{newunicodechar}
      \newunicodechar{ }{ }
      \usepackage[sharp]{easylist}
      \usepackage{adjustbox}
      \usepackage{comment}
      \usepackage[normalem]{ulem}
      \usepackage[T1]{tipa}
      \usepackage{xcolor}
      \usepackage{amsmath}
      \usepackage{hyperref}

    % floats

      \usepackage{tabularx}
      \usepackage{booktabs}
      \usepackage{multirow}
      \usepackage{float}
      \usepackage{array}
      \usepackage[]{caption}

    % code listings

      % code listings

        \usepackage{listings}
        \lstset{
          breaklines=true,
          basicstyle=\ttfamily
        }

    % todo notes

      \usepackage{todonotes}

    % references

      \usepackage[
        style=apa,
        backend=biber,
        doi=false, eprint=false, url=false, isbn=false
        ]{biblatex}
      \AtEveryBibitem{\clearfield{note}}
      \AtEveryCitekey{\clearfield{note}}
      \addbibresource{references.bib}
      \DeclareLanguageMapping{british}{british-apa}

  % new commands

    % formatting

      \newcommand{\se}[1]{\textbf{#1}}
      \newcommand{\hw}[1]{\se{#1}:}
      \newcommand{\sps}[1]{\textsuperscript{#1}}
      \newcommand{\sbs}[1]{\textsubscript{#1}}
      \newcommand{\supsub}[2]{\rlap{\sps{#1}}\sbs{#2}}
      \newcommand{\qpar}[1]{
        \paragraph{#1}
        \mbox{}
        \vspace{.5\baselineskip}
        }

    % linguistic markup

      \newcommand{\phn}[1]{{[\shorthandoff{"}\scantokens{\tipaencoding#1\endinput}}]}
      \newcommand{\phnm}[1]{{/\shorthandoff{"}\scantokens{\tipaencoding#1\endinput}}/}
      \newcommand{\grphm}[1]{\begin{math}\langle \end{math}\textsf{#1}\begin{math}\rangle \end{math}}
      \newcommand{\mrphm}[1]{\textsc{\{#1\}}}
      \newcommand{\ol}[1]{\emph{#1}}
      \newcommand{\mn}[1]{`{#1}'}
      \newcommand{\lxm}[1]{\textsc{\{#1\}}}

  % new environments

    \newenvironment{qitem}
      {
        \vspace{.5\baselineskip}
        \begin{easylist}[itemize]
        \ListProperties(Margin1=0em, Progressive*=2.5em, Space=.5em, Space*=.5em)
      }
      {
        \end{easylist}
        \vspace{.5\baselineskip}
      }

    \newenvironment{qenum}
      {
        \vspace{.5\baselineskip}
        \begin{easylist}[enumerate]
        \ListProperties(Margin1=0em, Progressive*=2.5em, Space=1em, Space*=1em)
      }
      {
        \end{easylist}
        \vspace{.5\baselineskip}
      }
